# Where the browser reaches the Egress gateway. Defaults work for local dev
# (the gateway runs on :8000 via `make gateway`).
NEXT_PUBLIC_GATEWAY_WS=ws://127.0.0.1:8000/ws/run
NEXT_PUBLIC_GATEWAY_HTTP=http://127.0.0.1:8000
